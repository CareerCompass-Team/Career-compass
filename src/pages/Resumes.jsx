export default function Resumes() {
  return (
    <div className="p-8 max-w-3xl mx-auto animate-fadeIn">
      <div
        className="rounded-xl p-8 text-center"
        style={{ background: 'var(--bg-card)', border: '1px dashed var(--border-2)' }}
      >
        <h1 className="font-display text-xl font-semibold mb-2" style={{ color: 'var(--text-1)' }}>
          Resumes
        </h1>
        <p className="text-sm" style={{ color: 'var(--text-4)' }}>
          Not built yet — this page is a placeholder so routing and navigation work.
        </p>
      </div>
    </div>
  )
}
